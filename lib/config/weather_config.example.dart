/// WeatherAPI.com API key for weather background feature.
///
/// To configure:
/// 1. Sign up at https://www.weatherapi.com/ (free tier)
/// 2. Copy your API key from the dashboard
/// 3. Copy this file to `weather_config.dart` (same directory)
/// 4. Replace the placeholder value below with your actual key
///
/// IMPORTANT: `weather_config.dart` is gitignored — never commit real credentials.
const String weatherApiKey = 'YOUR_API_KEY';
